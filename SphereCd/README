A file named Sphere.stl must be located in constant/triSurface
The sphere must have radius 0.5mm.

To run:

In linux go to this directory.

Set up geometry by typing these commands:

blockMesh

surfaceFeatureExtract

snappyHexMesh

Then, open the new folder called '3'. copy the contents into the constant folder. Finally run

simpleFoam